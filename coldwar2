import java.io.*;
import java.util.*;
import java.awt.*;

public class ColdWar2 {
   public static void main (String[] args)throws FileNotFoundException{
   int missile = 60;
   		/*	File inputFile = new File("ColdWarText.txt");
            Scanner fileInput = new Scanner(inputFile);
            //read from file
            while (fileInput.hasNextLine()) {
                  String line = fileInput.nextLine();
                  int length = line.length();
                  String[] letterPrinter = length;
            }
            
            */
                     System.out.println("you have a inventory of " + missile + " missiles.");
                     
                     move();
   }
   
   public String move(){
   //need to scan for a city name
         Scanner console = new Scanner(System.in);
         System.out.print("What city to attack now? ");
         String attackCity = console.next();
         if 
         return attackCity;
   }
}
